import 'package:flutter/material.dart';

///   all pages of list  ///
class Yemen extends StatelessWidget {
  const Yemen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    /// put page content of your list  here ///

    return Container(
        child: Center(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Image.asset('assets/yemen.png'),
          Text(
            'Yemen',
            style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
          ),
        ],
      ),
    ));
  }
}

class Egypt extends StatelessWidget {
  const Egypt({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    /// put page content of your list  here ///

    return Container(
      child: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Image.asset('assets/egypt.png'),
            Text(
              'Egypt',
              style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
            ),
          ],
        ),
      ),
    );
  }
}

class SaudiArabia extends StatelessWidget {
  const SaudiArabia({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      /// put page content of your list  here ///

      child: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Image.asset('assets/saudi arabia.png'),
            Text(
              'SaudiArabia',
              style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
            ),
          ],
        ),
      ),
    );
  }
}

class Iraq extends StatelessWidget {
  const Iraq({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      /// put page content of your list  here ///

      child: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Image.asset('assets/iraq.png'),
            Text(
              'Iraq',
              style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
            ),
          ],
        ),
      ),
    );
  }
}

class Algeria extends StatelessWidget {
  const Algeria({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      /// put page content of your list  here ///
      child: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Image.asset('assets/algeria.png'),
            Text(
              'Algeria',
              style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
            ),
          ],
        ),
      ),
    );
  }
}

class Tunisia extends StatelessWidget {
  const Tunisia({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      /// put page content of your list  here ///

      child: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Image.asset('assets/tunisia.png'),
            Text(
              'Tunisia',
              style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
            ),
          ],
        ),
      ),
    );
  }
}

class Syria extends StatelessWidget {
  const Syria({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      /// put page content of your list  here ///

      child: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Image.asset('assets/syria.png'),
            Text(
              'Syria',
              style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
            ),
          ],
        ),
      ),
    );
  }
}

class Jordan extends StatelessWidget {
  const Jordan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    /// put page content of your list  here ///
    return Container(
      child: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Image.asset('assets/jordan.png'),
            Text(
              'Jordan',
              style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
            ),
          ],
        ),
      ),
    );
  }
}

/// Thank you for watching..don't forget to subscribe to the channel and like the video. Good luck ^_^
